# practice-java

Simple Maven Java project to practice small exercises and REST Assured tests.

Build:

```powershell
mvn clean package
```

Run tests:

```powershell
mvn test
```

Run the app (using exec plugin):

```powershell
mvn exec:java -Dexec.mainClass=com.example.App
```
